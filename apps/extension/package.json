{
  "name": "tipote-extension",
  "private": true,
  "version": "1.12.5",
  "description": "Extension Chrome Tipote — boost LinkedIn collaboratif",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build:local": "node build.mjs --local",
    "build:firefox": "node build.mjs --firefox",
    "build:firefox:local": "node build.mjs --firefox --local",
    "dev": "node build.mjs --watch",
    "dev:local": "node build.mjs --watch --local",
    "dev:firefox": "node build.mjs --watch --firefox",
    "dev:firefox:local": "node build.mjs --watch --firefox --local",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "preact": "^10.22.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.21.5",
    "typescript": "^5.5.0"
  }
}
