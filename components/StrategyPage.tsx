// components/StrategyPage.tsx
// Composant neutre, juste pour ne plus avoir d'erreurs de build.
// La vraie page stratégie est dans app/strategy/page.tsx.

"use client";

export default function StrategyPage() {
  return (
    <div>
      {/* Composant StrategyPage inutilisé pour l'instant */}
    </div>
  );
}
